name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Render Deploy
        run: |
          curl -X POST https://api.render.com/deploy/srv-d29m4cur433s739hvg80?key=https://api.render.com/deploy/srv-d29m4cur433s739hvg80?key=5CeGMx0Rv6g
